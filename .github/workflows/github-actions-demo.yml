name: ReactJS NLW Together
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    env:
      REACT_APP_API_KEY: "AIzaSyB81Jtxxe9GfWo1byRW4HaujAWgc9e4ONI"
      REACT_APP_AUTH_DOMAIN: "reactjs-nlw-together.firebaseapp.com"
      REACT_APP_DATABASE_URL: "https://reactjs-nlw-together-default-rtdb.firebaseio.com"
      REACT_APP_PROJECT_ID: "reactjs-nlw-together"
      REACT_APP_STORAGE_BUCKET: "reactjs-nlw-together.appspot.com"
      REACT_APP_MESSAGING_SENDER_ID: "874086180232"
      REACT_APP_APP_ID: "1:874086180232:web:66ba529dd8d47c7418863d"
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v2
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."